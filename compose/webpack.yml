# run this with
# $ docker-compose -f docker-compose.yml -f compose/worker.yml up
version: '3'
services:
  web:
    volumes:
    # mount supervisor webpack config
    - ./configs/webpack/webpack.ini:/etc/supervisor.d/webpack.ini:ro
