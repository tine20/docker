# run this with
# $ docker-compose -f docker-compose.yml -f compose/macos.yml up
version: '2'
services:
  web:
    environment:
      # no generic way yet ;-(
      # https://github.com/docker/for-linux/issues/264
      XDEBUG_CONFIG: "remote_host=docker.for.mac.host.internal remote_enable=on remote_port=9001"
