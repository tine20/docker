passdb {
    driver = sql

    args = /etc/dovecot/dovecot-master-sql.conf.ext
    master = yes

    # Unless you're using PAM, you probably still want the destination user to
    # be looked up from passdb that it really exists. pass=yes does that.
    pass = yes
}